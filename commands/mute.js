module.exports = {
    name: "mute",
    description: "mute user",
    global: true,
    permissions: ["MANAGE_MESSAGES"],
    /**
     * @param {Discord.Message} message 
     * @param {Array} args 
     */  
	execute(message, args) {
        
    }
}